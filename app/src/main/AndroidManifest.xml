<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="co.edu.poli.gamification.poliplay">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SeleccionarCurso" />
        <activity android:name=".SeleccionarRol" />
        <activity android:name=".SeleccionarTransporte" />
        <activity android:name=".Mapa" />
        <activity android:name=".JuegoRelacionar" />
        <activity android:name=".JuegoRelacionarSolucion" />
        <activity android:name=".JuegoAhorcado"></activity>
    </application>

</manifest>